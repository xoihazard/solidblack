{
  "name": "HalftoneMaker",
  "version": "0.0.1",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.4.0",
    "browser-sync": "^2.18.8",
    "rollup": "^0.41.6",
    "rollup-watch": "^3.2.2",
    "rollup-plugin-commonjs": "^5.0.4",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-plugin-uglify": "^1.0.2",
    "regl": "^1.3.0"
  },
  "devDependencies": {
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "sync": "browser-sync start --server --startPath public --files='public/**/*'",
    "dev": "concurrently -k \"npm run watch\" \"npm run sync\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "xoihazard",
  "license": "ISC",
  "description": "HTML5 Hexagonal Halftone Maker",
  "homepage": "http://xoihazard.com"
}
